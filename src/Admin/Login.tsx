import usePageTitle from "../usePageTitle";

const Login = () => {
    usePageTitle('Login | Admin Dashboard');
    return (
      <div className="AdminLogin">
        <h1>Admin Login</h1>
      </div>
    );
  }